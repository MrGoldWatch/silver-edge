name: Deploy to Railway

on:
  push:
    branches: [ main, 2.0.0 ]
  pull_request:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: ${{ github.ref == 'refs/heads/main' && 'production' || 'development' }}
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        cache-dependency-path: backend/package-lock.json
        
    - name: Install backend dependencies
      run: |
        cd backend
        npm ci
      env:
        PRISMA_ENGINES_CHECKSUM_IGNORE_MISSING: 1
        
    - name: Build backend
      run: |
        cd backend
        npm run build
        
    - name: Run tests
      run: |
        cd backend
        npm test
        
    - name: Install Railway CLI
      run: npm install -g @railway/cli

    - name: Deploy to Railway (Development)
      if: github.ref == 'refs/heads/2.0.0'
      run: |
        cd backend
        echo "Deploying to Railway development environment..."
        railway up --service backend-dev --environment dev
      env:
        RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
        RAILWAY_PROJECT_ID: 4b38ca53-0032-446a-93bb-1d48eb303d9b

    - name: Deploy to Railway (Production)
      if: github.ref == 'refs/heads/main'
      run: |
        cd backend
        echo "Deploying to Railway production environment..."
        railway up --service backend-prod --environment production
      env:
        RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
        RAILWAY_PROJECT_ID: 4b38ca53-0032-446a-93bb-1d48eb303d9b

    - name: Run database migrations (Development)
      if: github.ref == 'refs/heads/2.0.0'
      run: |
        cd backend
        npx prisma migrate deploy
      env:
        DATABASE_URL: ${{ secrets.DEV_DATABASE_URL }}

    - name: Run database migrations (Production)
      if: github.ref == 'refs/heads/main'
      run: |
        cd backend
        npx prisma migrate deploy
      env:
        DATABASE_URL: ${{ secrets.PROD_DATABASE_URL }}
